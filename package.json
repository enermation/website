{
  "name": "enermation-workspace",
  "version": "0.1.2",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "cd web && bun run dev",
    "build": "cd web && bun run build",
    "start": "cd web && bun run start",
    "changeset": "cd web && bun run changeset",
    "version-packages": "cd web && bun run version-packages"
  }
}
